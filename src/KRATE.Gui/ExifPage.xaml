<UserControl
    x:Class="Krate.Gui.ExifPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="Title" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock x:Name="FileName" Opacity="0.7" />
            </StackPanel>
            <Button x:Name="OpenButton" Grid.Column="1" Click="OnOpen" VerticalAlignment="Center" Style="{StaticResource AccentButtonStyle}" />
        </Grid>

        <Border Grid.Row="1" CornerRadius="8" BorderThickness="1" Padding="20"
                AllowDrop="True" DragOver="OnDragOver" Drop="OnDrop"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <Grid>
                <TextBlock x:Name="DropHint" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Opacity="0.6" TextWrapping="Wrap" TextAlignment="Center" />
                <ScrollViewer>
                    <TextBlock x:Name="Result" FontFamily="Consolas" FontSize="14" IsTextSelectionEnabled="True" TextWrapping="Wrap" />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
