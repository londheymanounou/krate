<UserControl
    x:Class="Krate.Gui.FilePickerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock x:Name="Title" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock x:Name="SelectedPath" Opacity="0.7" TextTrimming="CharacterEllipsis" />
            </StackPanel>
            <Button x:Name="FileButton" Grid.Column="1" Click="OnPickFile" VerticalAlignment="Center" />
            <Button x:Name="FolderButton" Grid.Column="2" Click="OnPickFolder" VerticalAlignment="Center" Style="{StaticResource AccentButtonStyle}" />
        </Grid>

        <TextBlock Grid.Row="1" x:Name="Prompt" Opacity="0.7" TextWrapping="Wrap" />

        <Border Grid.Row="2" CornerRadius="8" BorderThickness="1" Padding="16"
                AllowDrop="True" DragOver="OnDragOver" Drop="OnDrop"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid>
                <TextBlock x:Name="DropHint" HorizontalAlignment="Center" VerticalAlignment="Center"
                           Opacity="0.55" TextWrapping="Wrap" TextAlignment="Center" />
                <ScrollViewer HorizontalScrollBarVisibility="Auto">
                    <TextBlock x:Name="Result" FontFamily="Consolas" FontSize="13" IsTextSelectionEnabled="True" TextWrapping="NoWrap" />
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
