<UserControl
    x:Class="Krate.Gui.BulkRenamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="Title" Grid.Row="0" Style="{StaticResource TitleTextBlockStyle}" />

        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Button x:Name="FolderButton" Grid.Column="0" Click="OnPickFolder" />
            <TextBlock x:Name="FolderPath" Grid.Column="1" Opacity="0.7" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="Find" Grid.Column="0" Header="Find" TextChanged="OnChanged" />
            <FontIcon Grid.Column="1" FontFamily="Segoe Fluent Icons" Glyph="&#xE72A;" FontSize="14" VerticalAlignment="Bottom" Margin="0,0,0,8" />
            <TextBox x:Name="Replace" Grid.Column="2" Header="Replace with" TextChanged="OnChanged" />
        </Grid>

        <Border Grid.Row="3" CornerRadius="8" BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ListView x:Name="Preview" SelectionMode="None" />
        </Border>

        <Grid Grid.Row="4" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="Status" Grid.Column="0" Opacity="0.7" VerticalAlignment="Center" />
            <Button x:Name="ApplyButton" Grid.Column="1" Click="OnApply" Style="{StaticResource AccentButtonStyle}" IsEnabled="False" />
        </Grid>
    </Grid>
</UserControl>
