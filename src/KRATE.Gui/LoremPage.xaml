<UserControl
    x:Class="Krate.Gui.LoremPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="Title" Grid.Row="0" Style="{StaticResource TitleTextBlockStyle}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
            <ToggleSwitch x:Name="Mode" Toggled="OnChanged" />
            <TextBlock Text="1" VerticalAlignment="Center" Opacity="0.6" />
            <Slider x:Name="Count" Minimum="1" Maximum="200" Value="50" Width="240" ValueChanged="OnChanged" VerticalAlignment="Center" />
            <TextBlock x:Name="CountLabel" VerticalAlignment="Center" MinWidth="80" />
            <Button x:Name="CopyButton" Click="OnCopy" />
        </StackPanel>

        <Border Grid.Row="2" CornerRadius="8" BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ScrollViewer>
                <TextBlock x:Name="Output" IsTextSelectionEnabled="True" TextWrapping="Wrap" Padding="16" LineHeight="22" />
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
