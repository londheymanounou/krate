<UserControl
    x:Class="Krate.Gui.GraphPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls">

    <Grid Padding="20" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="Title" Grid.Row="0" Style="{StaticResource TitleTextBlockStyle}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
            <TextBox x:Name="Fx" Header="y =" Width="240" Text="sin(x)" TextChanged="OnChanged" FontFamily="Consolas" />
            <muxc:NumberBox x:Name="XMin" Header="x min" Value="-10" Width="110" ValueChanged="OnChanged" />
            <muxc:NumberBox x:Name="XMax" Header="x max" Value="10" Width="110" ValueChanged="OnChanged" />
            <TextBlock x:Name="Error" Foreground="{ThemeResource SystemFillColorCriticalBrush}" VerticalAlignment="Bottom" />
        </StackPanel>

        <Border Grid.Row="2" CornerRadius="8" BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
            <Canvas x:Name="Plot" SizeChanged="OnResize" />
        </Border>
    </Grid>
</UserControl>
